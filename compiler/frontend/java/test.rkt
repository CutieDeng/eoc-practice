#lang racket/base

;; ============================================================
;; Java Frontend: 测试模块 (STUB)
;; ============================================================
;;
;; TODO: Reimplement tests when the Java pipeline is ready.
;; ============================================================

(require rackunit)
(require "reader.rkt")
(require "pipeline.rkt")
(require "../../kernel/ir/jvm/types.rkt")
(require "../../kernel/ir/cfg/types.rkt")

;; === Test placeholder ===

(module+ test
  (displayln "Java frontend tests - stubbed out pending migration"))
